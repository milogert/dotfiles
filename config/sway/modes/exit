# Game modes.
set $mode_exit "Exit: (y)es or (n)o?"
mode "$mode_exit" {
    bindsym n mode "default"
    bindsym Escape mode "default"
    bindsym $mod+Shift+e mode "default"
    bindsym y exit
}
bindsym $mod+Shift+e mode $mode_exit

