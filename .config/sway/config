# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# Commands must be first (defines mod).
include commands

# Configuration.
include colors
include display/outputs
include display/font
include display/windows
include workspaces
include volume

# Modes definition.
include modes/power
include modes/game
include modes/resize
include modes/launcher

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
    swaybar_command waybar --config ~/.config/waybar/top/config
}
bar {
    swaybar_command waybar --config ~/.config/waybar/bottom/config
}

# Touchpad tapping.
input "2:14:ETPS/2_Elantech_Touchpad" {
    tap enabled
    natural_scroll disabled
}
